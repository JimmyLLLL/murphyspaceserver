const config = {
    // 启动端口
    port: 4000,
  
    // 数据库配置
    database: {
      DATABASE: 'personal',
      USERNAME: 'root',
      PASSWORD: 'Jmspeed13.',
      PORT: '3306',
      HOST: 'localhost'
    }
  }
  
  module.exports = config